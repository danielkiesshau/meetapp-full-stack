import React from 'react';
import { Image } from 'react-native';
import logo from '../assets/logo.png';
// eslint-disable-next-line
const Logo = props => <Image source={logo} {...props} />;

export default Logo;
