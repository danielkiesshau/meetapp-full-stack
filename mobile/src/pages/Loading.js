import React from 'react';
import { Image } from 'react-native';
import splash from '../assets/splash.png';

const Loading = () => <Image source={splash} resizeMode="cover" />;

export default Loading;
